# 1.- Comenzar a hcer comparaciones de elementos adyacentes
# 2.- Repetir hasta tener una pasada completa sin ningun swap


def bubbleSort(x):
    n = len(x)
    for i in range(n):
        print(array)
        for j in range(0, n-i-1):
            if array[j] > array[j+1]:
                array[j], array[j+1] = array[j+1], array[j]


array = [190, 1200, 1, 2, 4, 55, 1000, 6, 800, -56, 0, 8]
bubbleSort(array)


print('Arreglo de forma ascendente es: ')
for i in range(len(array)):
    print("%d" % array[i]),
